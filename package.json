{
  "name": "espocrm-ext-better-columns",
  "version": "1.0.0",
  "description": "Column resizing, double-click auto-fit, reordering and persistent personal layouts for EspoCRM list views",
  "scripts": {
    "test": "node --test --test-reporter=spec src/tests/*.test.js",
    "sa": "vendor/bin/phpstan",
    "unit-tests": "vendor/bin/phpunit",
    "integration-tests": "node build --copy && node build --composer-install && cd site && php vendor/bin/phpunit tests/integration/Espo/Modules/BetterColumns",
    "copy": "node build --copy",
    "composer-install": "node build --composer-install",
    "sync": "node build --copy && node build --composer-install",
    "extension": "node build --extension",
    "clear-cache": "docker compose -f ../compose.yaml exec -T web php clear_cache.php",
    "rebuild": "node build --rebuild",
    "all": "node build --all",
    "prepare-test": "node build --prepare-test"
  },
  "type": "module",
  "author": "Alex Santin",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "espo-extension-tools": "github:espocrm/extension-tools#0.4.5",
    "fs-extra": "^9.0.0"
  },
  "engines": {
    "npm": ">=8",
    "node": ">=18"
  }
}
